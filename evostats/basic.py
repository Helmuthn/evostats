import math
def add(x: int, y: int) -> int:
    """Adds two inputs.

    Args:
        x: The first input
        y: The second input

    Returns:
        The sum of the two inputs
    """
    return x + y

def invalid_add(x: int, y: int) -> int:
    return math.sqrt(x+y)
